import { mockSearchResults } from '../mocks/searchResults'
import type { SearchService } from './search.service'
export const mockSearchService: SearchService = { search: (query, filters) => new Promise((resolve) => { window.setTimeout(() => { const results = mockSearchResults.filter((item) => (filters.categories.length === 0 || filters.categories.includes(item.category)) && (filters.fileTypes.length === 0 || filters.fileTypes.includes(item.fileType)) && item.similarityScore >= filters.relevance); resolve({ results, understanding: `I searched for information related to “${query}” across policies, handbooks, and operational guides.`, relatedQuestions: ['What expenses need manager approval?', 'What receipts are required for a claim?', 'What is the business travel policy?'] }) }, 900) }) }
