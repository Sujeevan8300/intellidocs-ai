import { MOCK_RESPONSE_DELAY_MS } from '../constants/chat.constants'
import type { ChatService } from './chat.service'
export const mockChatService: ChatService = { sendMessage: ({ conversationId, prompt }) => new Promise((resolve) => { window.setTimeout(() => resolve({ id: crypto.randomUUID(), conversationId, role: 'assistant', content: `## Here’s what I found\n\nBased on your organization’s knowledge base, **${prompt}**\n\nThe relevant guidance is documented in the current internal policies. I’ve highlighted the main points and included the supporting sources below.\n\n- Review the policy owner and effective date before acting\n- Follow the documented approval workflow\n- Contact People Operations if you need an exception\n\n\`Tip: You can ask a follow-up question to narrow this down.\``, createdAt: new Date().toLocaleTimeString([], { hour: 'numeric', minute: '2-digit' }), citations: [{ id: crypto.randomUUID(), documentName: 'Employee Handbook 2024', page: 12, excerpt: 'Current policy and approval guidance' }, { id: crypto.randomUUID(), documentName: 'Operations Playbook', page: 7, excerpt: 'Standard workflow' }] }), MOCK_RESPONSE_DELAY_MS) }) }
