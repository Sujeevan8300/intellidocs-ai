import { CopyOutlined, DislikeOutlined, LikeOutlined, ReloadOutlined } from '@ant-design/icons'
import { App, Tooltip } from 'antd'
export function MessageActions() { const { message } = App.useApp(); const copy = async () => { await navigator.clipboard.writeText('Response copied'); message.success('Response copied') }; return <div className="message-actions"><Tooltip title="Copy response"><button onClick={copy} aria-label="Copy response"><CopyOutlined /></button></Tooltip><Tooltip title="Helpful"><button aria-label="Like response"><LikeOutlined /></button></Tooltip><Tooltip title="Not helpful"><button aria-label="Dislike response"><DislikeOutlined /></button></Tooltip><Tooltip title="Regenerate"><button onClick={() => message.info('Regeneration will be available with the live AI service.')} aria-label="Regenerate response"><ReloadOutlined /></button></Tooltip></div> }
