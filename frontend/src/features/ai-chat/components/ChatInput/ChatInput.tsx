import { AudioOutlined, PaperClipOutlined, SendOutlined } from '@ant-design/icons'
import { Button, Input, Tooltip } from 'antd'
import type { KeyboardEvent } from 'react'
import { CHAT_MAX_LENGTH } from '../../constants/chat.constants'
export function ChatInput({ value, onChange, onSend, disabled }: { value: string; onChange: (value: string) => void; onSend: () => void; disabled: boolean }) { const keyDown = (event: KeyboardEvent<HTMLTextAreaElement>) => { if (event.key === 'Enter' && !event.shiftKey) { event.preventDefault(); onSend() } }; return <div className="chat-input-wrap"><div className="chat-input"><Input.TextArea value={value} onChange={(event) => onChange(event.target.value)} onKeyDown={keyDown} placeholder="Ask a question about your organization..." autoSize={{ minRows: 1, maxRows: 5 }} maxLength={CHAT_MAX_LENGTH} disabled={disabled} aria-label="Message IntelliDocs AI"/><div className="input-footer"><div><Tooltip title="Attachments coming soon"><Button type="text" disabled icon={<PaperClipOutlined />} aria-label="Attach file" /></Tooltip><Tooltip title="Voice input coming soon"><Button type="text" disabled icon={<AudioOutlined />} aria-label="Voice input" /></Tooltip><span>{value.length}/{CHAT_MAX_LENGTH}</span></div><Button type="primary" shape="circle" icon={<SendOutlined />} onClick={onSend} disabled={disabled || !value.trim()} aria-label="Send message" /></div></div><p>IntelliDocs AI can make mistakes. Verify important information in the source documents.</p></div> }
